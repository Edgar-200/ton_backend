"""
Patch: add send_password_reset_link to NotificationService.
This file is imported by apps/notifications/services.py at the bottom.
We write it as a standalone patch to avoid rewriting the whole service.
"""
# This module is intentionally empty — the method is added directly in services.py below.
